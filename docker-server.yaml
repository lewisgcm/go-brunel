persistence: mongo
mongo:
  uri:
  db:

notification: gitlab
gitlab:
  url: https://gitlab.com/api/v4/
  secret:

jwt:
  secret:
  default-role: admin

webhook:
  gitlab-secret:
  github-secret:

server-name:
secret:

oauth:
  gitlab:
    key:
    secret:
  github:
    key:
    secret:

remote:
  listen: 0.0.0.0:8885
  credentials:
    cert:
    key: